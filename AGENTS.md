# Hasad ERP (حصاد) — Project Rules

Flutter ERP application with Supabase backend and Riverpod state management. Arabic-first, RTL layout, bundled Cairo font. Every UI decision must follow `DESIGN_SYSTEM.md` exactly.

## Project Docs

Docs live at the repo root (`../../`). Read the relevant one before starting related work:

| File | When to read |
|---|---|
| `../../DESIGN_SYSTEM.md` | **Mandatory for ALL UI work** — colors, typography, spacing, radius, elevation, components, RTL, accessibility. No colors/spacing "by judgment" outside its tables. |
| `../../PROJECT_SPEC.md` | Product requirements and feature definitions |
| `../../SYSTEM_DESIGN.md` / `../../README_supabase_only.md` | Supabase schema, database design, and setup |
| `../../MILESTONES.md` | Phased roadmap + **source of truth for architecture** (M2 canonical structure, targets, SDK rules, error pattern) |

## Skills

Load the following skills via the skill tool when their domain applies. Do not skip them for matching work.

### ui-ux-pro-max
Use for designing, building, reviewing, or fixing any interface — pages, components, design system, accessibility, interaction, responsive layout, typography, color, charts, and Flutter UI implementation.

- Actual skill location: `C:\Users\khatib\.agents\skills\ui-ux-pro-max` (this is the source of truth).
- NOTE: the skill's own docs hardcode a `.claude/skills/...` path for its search script. Use the real one:
  `C:\Users\khatib\.agents\skills\ui-ux-pro-max\scripts\search.py`
- Run its Pre-Delivery Checklist (`references/pro-rules.md`) before delivering any app UI.

### flutter-apply-architecture-best-practices
Use when structuring the project, scaffolding a new feature, or refactoring for scalability.

- Skill location: `C:\Users\khatib\.claude\skills\flutter-apply-architecture-best-practices`
- ADAPT its examples to this project: it prescribes `ChangeNotifier`/`provider`/`get_it`, but Hasad uses **Riverpod** (`flutter_riverpod` + `riverpod_annotation`, codegen via `build_runner`). Follow its layering principles, but the canonical project structure is MILESTONES M2: `core/ domain/ data/ presentation/`. Implement state with Riverpod providers (`Notifier`/`AsyncNotifier`), not `ChangeNotifier`. The UI never imports the Supabase client (or drift) directly — it communicates with domain only through abstract repositories.

## Commands

Run everything from this directory (`Hasad-ERP/hasad_erp`) with the Flutter toolchain.

```bash
flutter pub get
flutter analyze   # must be clean before finishing any change
flutter test
```

Run/build/test commands that exercise the app's statement PDF **must add**
`--dart-define=use_arabic=true` (pdf's `use_arabic` is a compile-time const; without it Arabic
shapes as disconnected letters). Example: `flutter run -d chrome --web-port 5050 --dart-define=use_arabic=true`,
`flutter build web --dart-define=use_arabic=true`.

If Riverpod codegen classes are touched: `dart run build_runner build` (newer build_runner ignores the
old `--delete-conflicting-outputs` flag).

## Docs Upkeep Ritual (STANDING RULE)

After **every** finished milestone or slice, update the md docs in the same pass (before
declaring the step done). This keeps the next step fast and removes the need to re-derive
decisions. Checklist, in order:

1. `../../MILESTONES.md` — flip finished outputs to `✅`; mark the phase done in the phase map;
   add a **Handoff to next phase** note under the completed phase (what already exists the next
   phase reuses, what it must build fresh); append a row to **Appendix A — Implementation Log**
   with commit hash(es), DB objects added, test count, and any deviations.
2. This file (`AGENTS.md`) — record any behavior-changing facts discovered during the work
   (SDK API quirks, run commands, workflow rules, credentials, role splits). If only `this`
   step of the ritual has no facts, skip it.
3. `../../SYSTEM_DESIGN.md` §4 — sync the RPC/Edge Function table only when a contract changed
   (new/renamed RPC, dropped params). `security invoker` + balance assertion stays the rule.
4. `../../README_supabase_only.md` — only when the manual apply/test sequence changed (new
   migrations to paste in the SQL Editor, new `supabase/tests/*.ps1` to run).
5. `../../PROJECT_SPEC.md` — **never** edited by this ritual; it is the source of truth. Real
   divergences are logged in MILESTONES Appendix A instead.

PowerShell-only environment: no `&&` chaining — use `; if ($?) { ... }`.

## Stack & Conventions

- **Flutter** (Material 3), Dart SDK `^3.13.2`. Targets: **Android + Desktop only — no iOS/web.**
- **RTL + Arabic:** app is wrapped in `Directionality(TextDirection.rtl)` with `locale: Locale('ar')`. No screen is an exception. Numerals are always Western (0-9), never Arabic-Indic.
- **Cairo font** bundled at `assets/fonts/Cairo-Variable.ttf` (variable: 200–1000 weight). `GoogleFonts.config.allowRuntimeFetching = false` — never rely on runtime font downloads.
- **Supabase:** initialized once in `lib/main.dart` (url + publishableKey). Access via `Supabase.instance.client` — exclusively inside the `data/` layer's repositories; UI talks to abstract repositories only. `supabase_flutter` SDK only (no `dio`, no custom REST client); it attaches the session JWT automatically to `.rpc()`, `.from()`, and Edge Function calls.
  - **gotcha (supabase_flutter 2.17.2):** `.rpc()` uses named `params:` — `_client.rpc('fn', params: {...})`. A positional map fails to compile.
  - `DropdownButtonFormField` takes `initialValue:` in this Flutter version. Avoid `Iterable.firstOrNull` (needs `package:collection`).
- **Dev workflow:** native Android/Windows toolchains are broken on this machine — the verified run path is Chrome web: `flutter run -d chrome --web-port 5050` (app at `http://localhost:5050`). The `web/` directory is **local-only and never committed**. Validate final code with `flutter build web` (both with `--dart-define=use_arabic=true`).
- **DB operations:** the user applies SQL migrations manually in the Supabase **SQL Editor** (never run automatically here) and runs regression scripts manually with PowerShell from `supabase/tests/*.ps1`. So: write the migration into `supabase/migrations/`, write its `.ps1` test, then hand both to the user to apply/run.
- **Regression `.ps1` scripts must send JSON bodies as UTF-8 bytes** — Windows PowerShell 5.1 encodes a string `-Body` as ISO-8859-1, silently replacing Arabic with `?` on the wire (stored as real `0x3F` bytes). Every body call (post/patch to `.from()`, and RPC bodies) must go through a helper like `Invoke-Json` that does `[System.Text.Encoding]::UTF8.GetBytes(...)` + `-ContentType "application/json; charset=utf-8"`. The Flutter app (supabase_flutter) sends UTF-8 natively, so this only affects the scripts.
- **Never commit:** `.metadata`, `analysis_options.yaml`, `pubspec.lock`, `web/`, `build/`.
- **Dev test account:** `owner4@test.local` / `Test@1234567` (tenant owner4) on dev project `https://sxasnunzspkuwbxiddqd.supabase.co` — used by all `supabase/tests/*.ps1`.
- **Tab roles (as shipped):** Sales tab visible to all roles; Purchases, Inventory, Employees (الموظفون), and Salaries (الرواتب) tabs admin+accountant only. M7 reports tabs: dashboard (لوحة التحكم) is visible to all roles; دليل الحسابات، قيد اليومية، الأستاذ العام، ميزان المراجعة، القوائم المالية are admin+accountant only.
- **Money actions (as shipped):** payment recording, supplier settlement, and inventory adjustment are admin+accountant only (via `authStateProvider.value`); consignment invoices never show a payment button (RPC also rejects them).
- **`employees` RLS gap fixed in M6 (migration `0016`):** the M1 `employees` table shipped WITHOUT row-level security/policies/grants (unlike the 0004 master tables). Because the salary RPCs are `security invoker`, they silently fail on `select from employees` until 0016 is applied (RLS + all `authenticated` CRUD on own tenant, revoked from `anon`). The M6 employee screen and salary RPCs need this applied to work.
- **Master-table inserts stamp `tenant_id` via trigger (migration `0017`):** direct `.from('table').insert()` from the app AND the regression scripts never pass `tenant_id`, and nothing stamped it — so the RLS `with check (tenant_id = get_my_tenant_id())` saw `NULL` and PostgREST returned **403**. `0017_master_tables_set_tenant.sql` adds a `BEFORE INSERT` trigger `set_master_tenant()` (security invoker, `if new.tenant_id is null` guard — never stomps explicit values from security-definer RPCs) on employees/customers/suppliers/products/expenses. **Do not remove this trigger when writing new direct-insert tables; never assume direct inserts need a client-supplied tenant_id.**
- **Migration `0018` — consignment purchases of EXISTING products bind the supplier:** `create_sale_invoice` generates commission dues only when `product.supplier_id` points to a `deal_type='commission'` supplier, but `create_purchase_invoice` (0008) stamped `supplier_id`/`commission_rate` only on inline `new_product` items. Pre-created products therefore never earned commissions on their consignment sales. `0018_purchase_links_existing_product.sql` recreates `create_purchase_invoice` so a consignment receipt links an existing product to the purchasing supplier (`commission_rate` falls back to the supplier's); direct (owned) purchases never relink. Keep this behavior when editing the purchase or sale RPCs.
- **Test `.ps1` scripts keep a UTF-8 BOM:** Windows PowerShell 5.1 decodes a no-BOM script through a codepage-dependent path (Arabic lines can fail to parse). The Arabic regression scripts are saved UTF-8 **with BOM** — do not strip it. Related PS-only quirks already fixed: bodies must be UTF-8 bytes (above), RPC item arrays must be native hashtable arrays (never pre-serialized `ConvertTo-Json` strings), and **never wrap `Invoke-RestMethod` in `@(...)` for table GETs** — PS 5.1 already returns `Object[]` for multi-row responses, and the extra wrapper nests them to `.Count=1`, which silently breaks `Where-Object` row counting (e.g. `adjust_inventory.ps1` Case 5). GETs also don't need `Prefer: return=representation`. The same nesting hits RPC jsonb-array results: `@(Invoke-RPC -name "get_chart_of_accounts" -p @{})` reports `.Count=1` no matter how many rows — never wrap RPC array calls in `@(...)`; assign the result directly (flat `Object[]`) and read `.Count` off it. jsonb array **properties** of an envelope (e.g. `$dash.last_7_days`, `$row.lines`, `$dash.top_debtors`) parse flat with a correct `.Count` even inside `@(...)` pipelines; only the top-level `@(…)` around a command nests.
- **M6 employee statement is computed client-side (no statement RPC):** `buildEmployeeStatement` in `domain/salaries/salary_repository.dart` reads the read-only `salaries` + `employee_movements` + current `employees.base_salary`, and mirrors the 0010 arrears rule `max(base - paid, 0)` per earlier month. Since read-only tables are only queryable via the data layer, the reads live in `data/salaries/supabase_salary_repository.dart` while the pure aggregation stays in `domain/` (unit-tested).
- **`get_party_statement` RPC (migration `0015`):** `(p_party_type, p_party_id, p_from, p_to)` → one jsonb envelope `{party_type, party_id, from, to, opening, closing, lines[]}`; lines have `kind` `'invoice'|'payment'|'commission'`, `debit`, `credit` (running balance computed client-side); opening balance includes rows billed before `p_from`; commission dues are a separate `kind='commission'` line; consignment → zero debit/credit.
- **M7 report RPCs (migration `0019`, migration `0019_reports.sql`):** 9 report/write RPCs, all `security invoker`, revoked from `public`, execute granted to `authenticated` only — `get_dashboard_summary()` (KPI envelope incl. `last_7_days`, `top_debtors`, `low_stock`), `get_chart_of_accounts()`, `create_account(p_code, p_name, p_type, p_parent_code)`, `create_journal_entry(p_request_id, p_date, p_memo, p_lines jsonb)` (balanced-first + idempotent, `source_type='manual'`), `get_journal_entries(p_from, p_to)`, `get_ledger(p_account_id, p_from, p_to)` (`opening` + `lines[]` with running balance), `get_trial_balance(p_as_of_date)`, `get_income_statement(p_from, p_to)`, `get_balance_sheet(p_as_of_date)`. The chart-of-accounts/journal/report Flutter layers (`domain/{accounts,journal,reports}`, `data/{accounts,journal,reports}`, `presentation/providers/{accounts,journal,report}_providers.dart`) call these; the UI never hits `accounts`/`journal_entries` tables directly. Regression: `supabase/tests/reports.ps1`.
- **Flutter discovery: FontAwesomeIcons are `FaIconData`, not `IconData`** (font_awesome_flutter 11.0.0 / Font Awesome 7.2) — they cannot be passed to `Icon()` or `ButtonSegment.icon`, which want `IconData` (segments must drop icons or wrap in a `FaIcon`). Also `FontAwesomeIcons.arrowDownToBracket` and `arrowLeftFromBracket` DO NOT exist in this build; the debit icon is `arrowUpFromBracket` and the credit icon is `arrowRightToBracket`.
- **Flutter discovery: this project's Riverpod has NO `AsyncValue.valueOrNull`** — use `.maybeWhen(data: (v) => v ?? const [...], orElse: () => const [...])` instead of `watch(x.valueOrNull ?? [])`.
- **Flutter discovery: provider notifier class names can collide with domain model names** (e.g. `LedgerStatement`, `IncomeStatement`, `BalanceSheet` exist as BOTH a notifier in `report_providers.dart` and a model in `domain/reports/`). When a file imports both, alias the domain import (`import '.../ledger.dart' as ledger_models;`) or `hide` the notifier names on the providers import.
- **Widget-test discovery: the sidebar lazy-mounts its items.** `SideNavigation` renders via `ListView(children: ...)` but `SliverChildListDelegate` still only materializes in-viewport entries, so `find.text('أي عنصر')` returns nothing once a test scrolls the old anchor out of view (crashes `scrollUntilVisible` with "Bad state: No element"). Anchor side-nav scrolling with `find.descendant(of: find.byType(SideNavigation), matching: find.byType(Scrollable))`, not with a text anchor that itself scrolls away.
- **`pdf` 3.13 API (statement_pdf.dart):** embed a font via `pw.Font.ttf(ByteData)` + `pw.ThemeData.withFont(base:…, bold:…)`; set `pw.Page(textDirection: pw.TextDirection.rtl)` (shaping + RTL layout); render with `doc.save()` (not `build()`); there is **no** `PdfColor.opacity` — use precomputed tints; RTL table cells need explicit `pw.TextAlign.end`. Arabic shaping only runs when compiled with `use_arabic=true` and textDirection is rtl.
- **State management:** Riverpod only. Use `riverpod_annotation` codegen; delete stale `*.g.dart` via build_runner.
- **Error handling (fixed 3-catch pattern):** `PostgrestException`/RLS/constraint/RPC exceptions → `AppException`, `SocketException` → `Network`, `Exception` → `Unknown`. Never leak raw Supabase errors to the UI.
- **Roles:** admin / accountant / sales — screens hidden per role via the session.
- **Responsive nav:** fixed sidebar (>1100), collapsible sidebar (700–1100), swipeable Drawer (<700).
- **Icons:** `font_awesome_flutter` per DESIGN_SYSTEM §7 (not yet added to pubspec — add when first needed).
- **Spacing:** multiples of 4 only (`4, 8, 12, 16, 20, 24, 32, 40`). Card padding 20, form gap 12, section gap 24.
- **Dark mode:** out of scope per DESIGN_SYSTEM §13 — do not build a second ThemeData unless explicitly requested.
- **M8 Reminders — `send_reminders_to_all()` RPC contract (migration `0020`):** `security invoker`, `get_my_tenant_id()` guard, auto-seeds `tenant_settings` (webhook/message/threshold/enabled) if missing, ignores `reminder_enabled` (gates cron only), raises `لا يوجد رابط Webhook مضبوط للتنبيهات` if webhook empty/NULL, scans overdue mirroring `send_due_reminders` (sale + owned + remaining>0 + `date <= current_date - reminder_days_threshold` + phone present + HAVING NOT EXISTS same-day `reminder_log` with `lg.tenant_id = v_tenant`), loops `_fire_reminder`, returns `jsonb_build_object('sent', v_sent)`. Revoked from `public`, granted to `authenticated`.
- **M8 Reminders — PowerShell `$pid` reserved variable:** `$pid` is a read-only automatic variable in PowerShell 5.1 (process ID). Using it as a parameter name (e.g. `New-Sale -pid`) causes "Cannot overwrite variable pid because it is read-only or constant." Renamed to `$productId` in `supabase/tests/reminders.ps1`.
- **M8 Settings screen — `DropdownButtonFormField` uses `initialValue:`** (not `value:`) in this Flutter version; using `value:` does not set the initial selection correctly.
- **M8 Settings screen — `SwitchListTile` inside a decorated card** (Container with `BoxDecoration`) triggers "ListTile background color or ink splashes may be invisible" debug assertion. Fix: wrap the `SwitchListTile` in `Material(type: MaterialType.transparency)`.
- **M9 Double-Entry Engine — Pure Dart accounting layer** (`lib/domain/accounting/`) mirrors all 7 RPCs: `createSaleInvoice`, `createPurchaseInvoice`, `recordPayment`, `settleSupplier`, `addEmployeeMovement`, `paySalary`, `adjustInventory`. Each returns `DoubleEntryResult` with `JournalEntry`, validates balance via `isBalanced`, throws `UnbalancedEntryException` on mismatch. 19 unit tests cover all operations with correct Arabic account codes (1010/1015/1020/1030/2010/2015/2030/4010/4020/5010/5020/5030).
- **M9 Production Infra** — `supabase/production/` contains: `docker-compose.yml` for n8n+PostgreSQL on VPS, `.env.example`, `apply_migrations.ps1` (runs 0001-0021 via Supabase `exec_sql` RPC — note: `exec_sql` RPC may 404, SQL Editor paste is the verified path), `prod_schema.sql` (combined 0001–0021 single-paste script — hand-maintained: regenerate sections surgically, never full-rebuild), `backup.ps1` (pg_dump full/tenant-scoped), `OPERATIONS.md` runbook (daily/weekly/monthly checks, incident response, tenant onboarding), pgTAP suite (`supabase/tests/pgtap/run_pgtap_tests.sql` — 42 tests covering RLS isolation, double-entry balance, commissions, statements, idempotency, salaries, inventory, reminders, reports). `processed_requests` (generic idempotency ledger backing `settle_supplier`) is defined idempotently in migrations `0009` (CREATE TABLE, before the ALTERs) + `0011` (policies/grants) — its unique constraint on `request_id` must stay FULL (non-partial) because `settle_supplier` uses `on conflict (request_id)` as the arbiter.

- **M9 WS8 UI/UX Quality Pass (W8-7 `4dbc06c`, W8-8 `4f3c05d`):**
  - Header date chip visibility: driven by LayoutBuilder at the PageScaffold header row; shown when row width ≥700px (DESIGN_SYSTEM narrow breakpoint), hidden below. Prevents 375/768/1024 overflow and 1400 subtitle-wrap SnackBar overlap.
  - Flutter SDK quirk: `Flexible` is NOT const-constructible ("Cannot invoke a non-'const' constructor" at `Flexible`/`ConstrainedBox`).
  - Row inflexible children receive unbounded main-axis constraints → a `LayoutBuilder` inside a non-flex child sees `∞` and forces corner/chip to its cap. Fix: constrain at the Row level (LayoutBuilder on the Row), not inside the corner widget.
  - Reminders test sets viewport 1400×900 (not 800×600). SnackBar 4s timer does NOT advance under `pumpAndSettle`. Button center (842,846) was covered by still-visible SnackBar (rect 0,844–1400,900). W8-6 header placed button at y=822–836, clearing it.
  - Overflow diagnosis: `tester.takeException()` returns only the exception message, no creator. Set `FlutterError.onError` right before the pumping step, or temporarily remove the consume so the framework dumps "The relevant error-causing widget was: Row … creator … constraints … size". `FlutterErrorDetails` has NO `.renderObject` getter.
  - All 29 IconButtons verified to have tooltips (semantics labels). Live region added to reminder log feed (`Semantics(liveRegion: true)`). AutofillHints added to login, customer/supplier/employee/product forms, and settings webhook.
  - **Logo tile:** now the official squircle (`assets/branding/hasad-logo-icon.svg`) rendered **as-is** with `BoxFit.contain` across sidebar (32px via `BrandLogo`), splash (dark variant `hasad-logo-icon-navy.svg`), and login. The earlier "perfect circle" (`ClipOval`) and DESIGN_SYSTEM §5 radius-22 `ClipRRect` were dropped — the artwork's own `rx=112` squircle is the brand (user requested natural squircle). HTML reference uses the same SVG inline (hidden `#hasad-tile` symbol, `<use>` at login + sidebar), replacing Font Awesome `fa-wheat-awn` entirely — no wheat-awn trace remains anywhere.
  - **Inter-card/section spacing:** header→first-card reduced from 24→16 (now uniform `const gapAfterHeader = 16.0`). Dashboard stat grid gap 20→16; dashboard section-card stack gaps 20/24→16. Settings card stack gaps 24→16. DESIGN_SYSTEM §4 defines section gap as 24; tightened per user feedback that it was over the top.
  - **Header stays single-row app-wide; icon buttons are compact; mobile splits 50/50 and hides the avatar:** the two-row mobile header experiment was reverted after user feedback. Instead, the shared `iconButtonTheme` (`app_theme.dart`) shrinks every `IconButton` to `iconSize 18 / minimumSize 40×40 / padding 6` (was 24px glyph / 48×48 / 12). The `PageScaffold` header row (<700px) now uses a fixed **50/50 split**: the title gets `constraints.maxWidth / 2` (so e.g. `المبيعات` renders fully) and the remaining half holds the action icons in a single **one-line strip** — `FittedBox(BoxFit.scaleDown)` wrapping a `Row(mainAxisSize: min)` with 2px gaps, flush to the far edge (RTL = left). `FittedBox.scaleDown` guarantees icons never wrap or overflow (they only scale down on very narrow phones), so the chip + buttons always sit next to each other on one line. `UserCorner` (the avatar circle) is **dropped entirely on <700px** so the mobile header is a single tape with no circle; wide (≥700px) keeps `UserCorner(showDate: true)`. `FreshnessChip` detects narrow via **`MediaQuery.sizeOf(context).width < 700`** (NOT a LayoutBuilder — inside the FittedBox it receives unbounded width) and drops its clock icon + tightens padding on narrow so the group fits the half. The 2px between-icon gap is a user-requested deviation from DESIGN_SYSTEM's multiples-of-4 spacing. Slight touch-target deviation from 48px (40px) — user-approved. It applies everywhere via the theme (headers, search clears, range-bar reset, table row buttons); doesn't affect FAB, avatar, date chips, or form `prefixIcon`s. Verified by the responsive sweep test at 375/768/1024/1440 (240/240 tests).
- **Pushed fullscreen routes must own a `Scaffold`:** `MaterialPageRoute` does NOT insert a `Material` ancestor (unlike `showModalBottomSheet`, which does). A pushed page whose build starts with `Container → SafeArea → …` crashes the moment any `DropdownButtonFormField`/`InkWell`/`TextButton`/`ElevatedButton`/tooltip/`TextFormField` renders ("No Material widget found"). The New Sale/`Purchase` invoice pages shipped with `Container` instead of `Scaffold` — fixed by swapping to `Scaffold(backgroundColor: AppColors.background, body: SafeArea(…))` (identical visuals; also makes the route's own `ScaffoldMessenger` snackbars appear). Regression: `test/ui_invoice_forms_test.dart` (pumps both lists in a `Scaffold`, taps the FAB, asserts the form's RouteHeader renders with `tester.takeException()` null). Note: the new-purchase page's RouteHeader `title:`/`subtitle:` were stored as literal U+FFFD bytes (corrupted save) and are now real Arabic. `flutter test` works on this machine again (166 → **168** tests).
- **M9 Track 2 backend suite facts (commit `d25dc1a`, extended in the S5 pass):**
  - `supabase/tests/m9_full_suite.ps1` = **47 assertions** (17 Invoke-Ok + 18 Assert-Equal + 11 Assert-True + 1 Invoke-ExpectFail). It is double-click safe: `Test-Summary` accumulates a failure flag instead of calling `exit 1`; the script end does `if (-not [Console]::IsInputRedirected) { Read-Host ... }` then `if ($script:failed) { exit 1 }; exit 0`.
  - It is parameterized for any project: `-SupabaseUrl` / `-AnonKey` / `-OwnerAEmail` / `-OwnerAPass` / `-OwnerBEmail` / `-OwnerBPass`. Defaults point at DEV — the SAME suite runs against prod with `-SupabaseUrl https://... -AnonKey ...`.
  - T11 tests migration 0021: `switch_tenant(own)` succeeds, `users.current_tenant_id` persists, `get_my_tenant_id()` re-resolves, `switch_tenant(foreign)` is rejected (`Invoke-ExpectFail`). `user_tenants` has NO grants/RLS in 0021 — membership is proven via `switch_tenant`, never by direct select.
  - `_fire_reminder.p_amount` is `numeric` (was `bigint`): callers pass `sum(remaining)` (numeric), so `bigint` raised `42883`. The `reminder_log` insert casts `p_amount::bigint`. Any `revoke ... _fire_reminder(...)` must use the `numeric` signature.
  - `commission_rate` is a **percent 0-100** (the suite passes `20`), not a fraction.
  - Consignment receipts intentionally keep `remaining = total`, `status='unpaid'`, and post **no journal/AP** (verified in T3 and `sample_month.ps1`).
  - `get_journal_entries` exposes **`entry_id`** (not `id`/`source_id`) and returns rows ordered `entry_no desc`. Payment journals use `source_type='auto'`.
  - `processed_requests.request_id` unique constraint must stay FULL (non-partial) — `settle_supplier` uses `on conflict (request_id)`.
  - No COGS is booked automatically: the sale RPC posts revenue-only (Dr AR/cash/bank, Cr 4010) and purchases post Dr 1030 inventory only. `5010` stays 0 unless posted by a manual journal — this is the documented schema design, so reconciliation math must mirror it.
  - DEV was missing RLS enforcement; `supabase/production/reapply_rls.sql` (enable+force RLS + policies, no grant changes) and `reapply_reminder_rpcs.sql` (drop ALL reminder overloads by name, recreate numeric versions + grants + cron) are the remediation scripts; reuse them for prod hardening.
  - `supabase/production/verify_security.sql` = the README §7 checklist as SQL (10 blocks; run as `postgres`). `SECURITY DEFINER` allowlist: `get_my_tenant_id`, `seed_chart_of_accounts`, `register_tenant`, `seed_tenant_settings`, `_fire_reminder`, `send_due_reminders`, `switch_tenant`.
  - `supabase/tests/sample_month.ps1` (UTF-8 BOM) = M9 output #4 + S4 onboarding smoke: signup fresh auth user → `register_tenant` → one full past month (capital 1010/1015/3010, 3 direct purchases 1030, 1 consignment receipt, 3 credit sales, 2 payments, 2 manual-journal expenses 5020, 1 salary 5030) → reconciles all 14 seeded accounts, stock (P1=39/P2=28/P3=42), AR (CC=56000), commission dues (76800/19200), statement closing, trial-balanced, check=0, net +22000.
  - `backup.ps1` rewritten: PGPASSWORD set on process; real gzip via `GZipStream` (not Compress-Archive); tenant-scoped dumps are `--data-only --column-inserts [--on-conflict-do-nothing]` and NEVER `--clean` (restoring must not drop other tenants).
  - **Flutter 3.47.2 + objective_c 9.5.0 native assets issue:** `path_provider_foundation` pulls `objective_c ^9.2.1` (resolved 9.5.0) which ships `hook/build.dart`. The hook correctly returns early on non-iOS/macOS, but Flutter's build system validates the Windows MSVC compiler paths (cl.exe/link.exe/lib.exe/vcvars64.bat) *before* the hook runs. With broken MSVC toolchain (`flutter test` fails on this machine). Workaround: run `flutter build web` for compilation verification; `flutter analyze` for static checks. Tests (166/166) previously passed pre-dep; pure spacing/radius changes don't affect test logic.
- **M10 Offline & Sync — Slice A (drift local store core, 2026-09-12):** deps `drift` 2.35.0 + `sqlite3` 3.5.2 + `sqlite3_flutter_libs` 0.6.0+eol + `path_provider` 2.1.6 + `path` 1.9.1 (runtime), `drift_dev` 2.35.0 (dev). `lib/data/offline/local_database.dart` = `AppDatabase` with 16 server-mirror tables + `tenant_settings` + `report_cache` + `sync_queue` (localId, entityType, entityId, op, payload, status 'pending'|'staged'|'synced'|'failed', attempts, timestamps) + `id_map` (local uuid ↔ official id), schemaVersion 1, all tables partitioned by `tenant_id`. `lib/data/offline/local_store.dart` = abstract `LocalStore` (mirror/upsert/read/queue/idMap/reportCache/clearTenant helpers) + `DriftLocalStore` + `NullLocalStore` (web/no-op degradation) + `localStoreProvider` (FutureProvider). DB factory is a **conditional import** (`offline_factory.dart` → `offline_factory_io.dart` on `dart.library.io`, else `offline_factory_stub.dart`) so `flutter build web` stays green with drift completely out of the web bundle.
  - **Drift 2.35 API facts:** `NativeDatabase` + `NativeDatabase.memory()` live in `package:drift/native.dart` (NOT `package:drift/drift.dart`); `int64()` maps to Dart `BigInt` — money columns must use `integer()` to stay `int`; select statements have NO `.and()` (combine row constraints with `&`); `InsertStatement` has NO `insertAll` (multi-row inserts go through `batch((b) { b.insertAll(...); })`); the drift-exported `isNull` column compare collides with `package:matcher` in test files — import with `hide isNull` (from drift) or vice-versa. Regenerate generated DB code with `dart run build_runner build`.
  - **Verification:** `flutter analyze` clean; `flutter test` **178/178** (168 baseline + 10 new `test/data/offline/local_store_test.dart` using in-memory drift, incl. cascade delete invoice→items, queue ordering/status transitions, id_map both directions, report-cache + settings null fallback, tenant isolation via clearTenant); `flutter build web --dart-define=use_arabic=true` green.
- **M10 Slice B — offline read caching (2026-09-12):** every data repository now wraps as `OfflineXRepository(this._inner, {required this.store, required this.tenantId})` in `lib/data/offline/` — `_inner` typed on the abstract domain interface for testability (invoice wrapper keeps the concrete `InvoiceRepository` class; fakes `implements` it). Masters (customer/supplier/product/employee) read the local mirror via `cacheFirst`; financials (account/invoice/journal/report/dashboard/statement/debts) use cache-last JSON via `cacheLast` under keys from `lib/data/offline/report_keys.dart` (+ model `fromJson`). Offline fallback fires ONLY on `NetworkException`; mirror/cache writes are best-effort (swallowed); `cacheLast` without cache rethrows; `cacheFirst` over a store with an empty mirror serves `[]`. Providers in lib/…/providers pass `store: ref.watch(localStoreProvider.value)` + `tenantId:` positionally; `offline_providers.dart` supplies `reportCachedAt(key)` (used by `FreshnessChip`, wired into 9 screens). Drift rows need `import 'local_database.dart'`; serialize `InvoiceOwnership` as `ownership.name` (no `dbValue`). Verification: analyze clean, **199/199** tests (178 + 10 offline_reads + 11 offline_wrappers with `_Fake*` repos), web build green.
- **M10 Slice C — master write rewiring (2026-09-17):** `OfflineWriteCoordinator` now owns ALL master CRUD — `writeX/updateX/deleteX` for customer/supplier/product/employee mirror locally with `synced:false` and enqueue a `SyncQueueRow` with `op: 'table_crud'` (replayed against `.from(<entity>)` by the future flush service); the RPC legs stay `op: 'rpc'`. Facts learned the hard way (all verified byte-exact at this commit):
  - **`LocalStore` has NO soft-delete / NO master delete at all** — the only delete-like method is `deleteInvoice(String id)`. The four `deleteX` legs DROP the mirror row deletion entirely and only enqueue `{op: 'table_crud', params: {'id': id}}`; the mirror row stays until the flush service replays the delete against the server. Do not introduce `softDelete*` on `LocalStore` to "fix" this.
  - **`sync_queue.op` is a required drift column** (`TextColumn op = text().withDefault(const Constant('rpc'))`): the generated `SyncQueueRow` ctor *still* requires a named `op:` argument (DB defaults don't relax the Dart row ctor). Every `enqueue(SyncQueueRow(...))` must pass `op` explicitly — RPC legs `'rpc'`, master legs `'table_crud'` (via the `_enqueueWrite(rpc:, params:, requestId:, entity:, localId:, op:)` helper added alongside `_enqueueRpc`).
  - **`Product`/`Employee`/`Supplier` have NO `toJson()` (only `fromJson`); `Customer` has `toJson()`.** The coordinator enqueues `draft.toJson()` (`ProductDraft`/`EmployeeDraft`/`SupplierDraft` all have `toJson`) — never the domain entity's.
  - **Domain shapes:** `ProductDraft` = name, barcode?, unit, unitType (`ProductUnitType`), salePrice/purchasePrice int, qty/reorderLevel double, supplierId?, commissionRate? — no category/costPrice. `EmployeeDraft` = name, jobTitle?, phone?, baseSalary int — no email/salaryBase. `Product` has no createdAt; `Employee` does.
  - **`ProductDraft` name collision:** `lib/domain/accounting/invoice_lines.dart` declares its OWN `ProductDraft` (the purchase inline-new-product draft with `unitType` as String + `costPrice`/`qtyOnHand`), which conflicts with `domain/products/product_draft.dart`. Any file importing both must `hide ProductDraft` on the invoice_lines import (offline_write.dart does this); `PurchaseLineDraft.newProduct` is a `NewProductDraft` from `purchase_invoice_draft.dart`, unrelated.
  - **Outdated env note corrected:** `flutter test` (232/232, incl. new master-write regression tests) and `flutter build web --dart-define=use_arabic=true` both run green on this machine — the M9 `objective_c`/MSVC native-assets `flutter test` failure does NOT currently reproduce.